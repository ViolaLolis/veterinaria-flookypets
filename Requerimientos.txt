//Modulos para que funcione el proyecto
npm i react-scripts 
npm install recharts
npm install @emailjs/browser
npm install react-dom

Usuarios de prueba
Admin: admin@example.com | 1234

Veterinario: vet@example.com | 5678

Usuario: user@example.com | abcd


//Para agregar todos los archivos que tenemos y los de mi compañero
git add .


//Para aregar comentario debemos poner
git commit -m "el comentario que vayas hacer entre comillas"

//Para actualizar el proyecto que haya echos los otros compañeros si o si se debe hacer
git pull origin main

//Ahora para lanzazr el proyecto debemos poner
git push origin main

// ahora vamos a instalar sus dependencias del server
npm install mysql2
npm init -y
npm install express mysql cors dotenv

Para luego ejecutar el servidor para que quede activo
node server.js
luego aparecera un link, dale control+click y les abrira una pagina, les tiene que aparecer pagina abierta con exito

Luego seleccionamos la carpeta del servidor//
cd servidor.js
npm install

Para luego ejecutar el servidor para que quede activo
node server.


Debera aparecerte Servidor backen corriendo, eso significa que ya esta corriendo con la base de datos

npm install axios (Vamos a usar axios para hacer solicitudes HTTP)

npm install bcrypt (para que las contraseñas no se muestren)

npm install framer-motion
npm install react-calendar
npm install @fortawesome/free-brands-svg-icons
npm install react-big-calendar moment
npm install chart.js react-chartjs-2

Comando para permisos sin perfil
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser